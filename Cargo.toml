[package]
name = "GeneAlgoLib"
version = "0.1.0"
edition = "2021"

[lib]
name = "fast_ga"
crate-type = ["cdylib"]

[dependencies]
# Both pyo3 and numpy MUST be on compatible version branches
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"
rand = "0.8.5"